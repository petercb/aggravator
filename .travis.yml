language: python
python:
  - "2.7"
addons:
  apt:
    packages:
      - jq
install:
  - python setup.py install
script:
  - inventory --vault-password-file=/dev/null --uri=example/config.yml --env=prod --list > output.json
  - jq -S . output.json
